
# In order to to create pages it is necessary to define routes for them.
# A route maps a URL path to a controller. It defines with what function
# or method will be called when a URL is accessed.
# If the user accesses http://drupal8.dev/view-session/{session}, the routing
# system will look for a route with that path. In this case it will find a
# match, and execute the _controller callback. In this case the callback is
# defined as a classname
# ("\Drupal\studio_photodesk_screens\Controller\ViewSessionController")
# and a method ("content").
studio_photodesk_screens.view_session_controller_content:
  path: '/view-session/{nid}'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewSessionController::content'
  requirements:
    _permission: 'access content'

# In order to to create pages it is necessary to define routes for them.
# A route maps a URL path to a controller. It defines with what function
# or method will be called when a URL is accessed.
# If the user accesses http://drupal8.dev/view-product/{product}, the routing
# system will look for a route with that path. In this case it will find a
# match, and execute the _controller callback. In this case the callback is
# defined as a classname
# ("\Drupal\studio_photodesk_screens\Controller\ViewProductController")
# and a method ("content").
studio_photodesk_screens.view_product_controller_content:
  path: '/view-product/{nid}'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewProductController::content'
  requirements:
    _permission: 'access content'


# In order to to create pages it is necessary to define routes for them.
# A route maps a URL path to a controller. It defines with what function
# or method will be called when a URL is accessed.
# If the user accesses http://drupal8.dev/view-session/{session}, the routing
# system will look for a route with that path. In this case it will find a
# match, and execute the _controller callback. In this case the callback is
# defined as a classname
# ("\Drupal\studio_photodesk_screens\Controller\ViewSessionController")
# and a method ("content").
studio_photodesk_screens.view_all_sessions_controller_content:
  path: '/view-sessions2'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewAllSessionsController::content'
  requirements:
    _permission: 'access content'

studio_photodesk_screens.view_products_controller_content:
  path: '/view-products2'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewAllProductsController::content'
  requirements:
    _permission: 'access content'

studio_photodesk_screens.view_dasboard_content:
  path: '/photodesk-dashboard'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewDashboard::content'
  requirements:
    _permission: 'access content'

studio_photodesk_screens.view_models_controller_content:
  path: '/view-models'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewAllModelsController::content'
  requirements:
    _permission: 'access content'

studio_photodesk_screens.view_PDP_controller:
  path: '/view-pdp/{nid}'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewPDPController::content'
  requirements:
    _permission: 'access content'

studio_photodesk_screens.dashboard:
  path: '/dashboard'
  defaults:
    _controller: '\Drupal\studio_photodesk_screens\Controller\ViewDashboard::content'
  requirements:
    _permission: 'access content'
